#include<stdio.h>

int main()
{
    float a,b,area;
    printf("enter base of triangle:");
    scanf("%f",&a);
    printf("enter height of triangle:");
    scanf("%f",&b);
    area=((a*b)/2);
    printf("area of triangle=%f",area);
}    